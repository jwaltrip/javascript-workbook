'use strict';

function printListOfOlympians(inputArr) {
  inputArr.forEach((item) => {
    console.log(item);
  });
}

const names = ['Jake', 'Christelle', 'Lori', 'MichaelPhelps', 'Olympus'];

printListOfOlympians(names);